"""
so101 — shared library for SO-101 sim-to-real pipeline.

Sub-packages:
  so101.curate       — telemetry curation and dataset loading
  so101.model        — PretrainCnn model and multi-task losses
  so101.viz_cnn      — interactive CNN training data visualizer
  so101.viz_pipeline — interactive image-pipeline tuning viewer
  so101.utils        — feature extractors and image-processing pipeline
"""

__version__ = "0.1.0"
